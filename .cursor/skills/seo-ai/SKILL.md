# SEO / AI optimization (Majico)

Apply when setting metadata or structuring pages for discoverability.

- One clear H1 per page; brand wordmark may precede H1 on the landing hero.
- Unique `title` + `description` per route; set `metadataBase` and canonical URLs to `https://klaut.pro`.
- Open Graph locale `de_DE` for primary German surface.
- Blog posts: article OG type, publishedTime, descriptive teasers without keyword stuffing.
